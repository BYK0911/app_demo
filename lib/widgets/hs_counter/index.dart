import 'package:flutter/material.dart';

class HsCounter extends StatelessWidget {
  const HsCounter({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('counter widget demo');
  }
}
